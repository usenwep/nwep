@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

find_dependency(OpenSSL)

# ngtcp2 and blst don't ship cmake configs, so consumers must set paths
# or use the pre-built package which includes all dependencies.

include("${CMAKE_CURRENT_LIST_DIR}/nwepTargets.cmake")

check_required_components(nwep)
